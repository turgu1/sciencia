.legend
  Document Categories
  = render 'new_link'

%table.dtable{ data: { source: document_categories_path(format: :json), sort_column: 2, sort_order: 'asc' } }
  %thead
    %tr
      %th.hleft Caption
      %th.hcenter Abbreviation
      %th.hcenter Order  
      %th.no-sort.buttons

  %tbody
