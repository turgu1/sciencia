replace_content(
    '#modal-info',
    "<%= j(render(
        :partial => 'pub_list_params',
        :object => @person,
        :locals => {
            :target_url => @target,
            :the_format => @format
        })) %>");
$('#modal-dialog').modal({ backdrop: false});